---
alwaysApply: true
---

# Auto-sync Mono Repository

## Rule
Before any code query or analysis, pull the latest changes from the local `mono` repository.

## Implementation
```bash
cd mono && git pull && cd ..
```

## Purpose
Ensures all code queries work with the latest version from the main branch.